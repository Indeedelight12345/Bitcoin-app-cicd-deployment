apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitcoin-app.fullname" . }}
  labels:
    app: {{ include "bitcoin-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "bitcoin-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
